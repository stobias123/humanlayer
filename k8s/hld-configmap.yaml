apiVersion: v1
kind: ConfigMap
metadata:
  name: hld-config
  namespace: humanlayer
  labels:
    app.kubernetes.io/name: hld
    app.kubernetes.io/component: daemon
data:
  HUMANLAYER_DAEMON_HTTP_PORT: "7777"
  HUMANLAYER_DAEMON_HTTP_HOST: "0.0.0.0"
  HUMANLAYER_DATABASE_PATH: "/data/daemon.db"
  HUMANLAYER_LOG_LEVEL: "info"
  HUMANLAYER_API_BASE_URL: "https://api.humanlayer.dev/humanlayer/v1"
