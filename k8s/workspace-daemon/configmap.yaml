apiVersion: v1
kind: ConfigMap
metadata:
  name: workspace-daemon-config
  namespace: humanlayer
  labels:
    app.kubernetes.io/name: workspace-daemon
    app.kubernetes.io/component: orchestrator
data:
  WORKSPACE_HTTP_PORT: "8888"
  WORKSPACE_HTTP_HOST: "0.0.0.0"
  WORKSPACE_DATABASE_PATH: "/data/workspace-daemon.db"
  WORKSPACE_HELM_CHART_PATH: "/opt/helm/hld-workspace"
  WORKSPACE_LOG_LEVEL: "info"
