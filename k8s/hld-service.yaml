apiVersion: v1
kind: Service
metadata:
  name: hld
  namespace: humanlayer
  labels:
    app.kubernetes.io/name: hld
    app.kubernetes.io/component: daemon
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 7777
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: hld
