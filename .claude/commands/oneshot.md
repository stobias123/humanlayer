---
description: Research ticket and launch planning session
---
1. Ensure that the directory your in has the latest changes from main via a `git pull`
2. use SlashCommand() to call /ralph_research with the given ticket number
3. launch a new session with `hlyr launch --model opus --dangerously-skip-permissions --dangerously-skip-permissions-timeout 30m --title "plan ENG-XXXX" "/oneshot_plan ENG-XXXX"`
